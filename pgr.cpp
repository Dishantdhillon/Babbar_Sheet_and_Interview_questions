#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    int arr[n];
    for(int i=0; i<n; i++) {
        cin >> arr[i];
    }
    int sum = 0, count = 0;
    if(arr[0] > arr[1]) {
        arr[0] = 2;
    }
    else {
        arr[0] = 1;
    }
    for(int i=1; i<n-1; i++) {
        if(arr[i] )
    }
    return 0;
}