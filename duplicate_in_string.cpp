#include<bits/stdc++.h>
using namespace std;

void duplicates(string s) {
    int n = s.length();
    /*unordered_set<char> set;
    for(int i=0; i<n; i++) {
        set.insert(s[i]);
    }
    for(int i=0; i<n; i++) {
        int count = 0;
        while(set.find(s[i]) != set.end()) {
            count++;
        }
        if(count > 1) {
            cout << s[i] << " " << count << "\n";
        }
    }*/
    int count[];
}



int main() {
    string s;
    cin >> s;
    duplicates(s);
    return 0;
}